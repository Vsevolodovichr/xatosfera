# Environment variables for CRM Application

# Cloudflare Worker API URL
# For local development:
VITE_API_URL=http://localhost:8787

# For production (replace with your actual Worker URL):
# VITE_API_URL=https://crm-api.your-subdomain.workers.dev

# Legacy Supabase variables (no longer needed, kept for reference)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
